numBase = float(input("\nInforme o valor da base do cálculo: "))
numExpoente = float(input("Informe o valor do expoente do cálculo: "))

print(f"\n{numBase:.2f} ** {numExpoente:.2f} = {numBase**numExpoente:.2f}\n")